### Counter Contract
The Counter contract now includes a `decrement` function to decrease the counter value.
Available functions:
- `increment()`: Increases the counter value.
- `decrement()`: Decreases the counter value.